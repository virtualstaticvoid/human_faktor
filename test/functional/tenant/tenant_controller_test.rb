require 'test_helper'

module Tenant
  class TenantControllerTest < ActionController::TestCase

    # NOTE: controller is essentially abstract, although it imposes some before_filters

    test "should check whether account is active" do
      pending
    end

    test "should check whether employee has a password" do
      pending
    end

  end
end
