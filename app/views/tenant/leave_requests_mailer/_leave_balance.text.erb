Leave Balance Details:

  Balance as at:    <%= @leave_request.date_from_s %><% if @leave_balance.leave_type.can_take_on? && @leave_balance.take_on != 0 %>
  Take On:          <%= pluralize(format_duration(@leave_balance.take_on), 'day') %><% end %><% if @leave_balance.leave_type.can_carry_over? && @leave_balance.carried_forward != 0 %>
  Carried Forward:  <%= pluralize(format_duration(@leave_balance.carried_forward), 'day') %><% end %>
  Allowance:        <%= pluralize(format_duration(@leave_balance.allowance), 'day') %>
  less Leave taken: <%= pluralize(format_duration(@leave_balance.leave_taken), 'day') %>
  Leave available:  <%= pluralize(format_duration(@leave_balance.available), 'day') %>
  less Requested:   <%= pluralize(format_duration(@leave_request.duration), 'day') %>
  Leave balance:    <%= pluralize(format_duration(@leave_balance.projected), 'day') %>

