$('#leave_balance').html('<%= escape_javascript(render(
        :partial => 'tenant/leave_requests/leave_balance', 
        :locals => { 
          :leave_type => @leave_type, 
          :date_from => @date_from, 
          :half_day_from => @half_day_from, 
          :date_to => @date_to,
          :half_day_to => @half_day_to })) %>');
$('#ajax_working').css('display', 'none');
