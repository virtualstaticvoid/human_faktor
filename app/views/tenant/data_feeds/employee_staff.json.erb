<% first = true %>
[
<% @employees.each do |employee| %><%= ',' unless first %>  { 
    "id": "<%= employee.id %>", 
    "label": "<%= employee.full_name %>",
    "value": "<%= employee.full_name %>"
  }
<%  first = false
  end %>
]
