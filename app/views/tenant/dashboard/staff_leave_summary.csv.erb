<%

  columns = ['employee']

  @leave_types.each do |leave_type|
    columns << leave_type.to_s.downcase
  end

%><%= columns.join(',').html_safe %>
<% for employee in @filter.employees %><%

  data = [employee.full_name]

  @leave_types.each do |leave_type|
    data << @filter.summary_for(employee, leave_type)
  end

%><%= data.join(',').html_safe %>
<% end %>