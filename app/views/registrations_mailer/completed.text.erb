Hello <%= @registration.full_name %>,

Welcome to <%= AppConfig.title %>. Your account for <%= @registration.title %> is ready!

You can use the following URL to access your account:

  <%= dashboard_url(:tenant => @registration.subdomain) %>

Next steps:

* Setup your company logo and theme,
* Optionally, create locations and departments,
* Update leave policies and allowances,  
* Add staff and optionally specify leave take on balances.

To complete your account setup, please follow this link:

  <%= account_setup_url(:tenant => @registration.subdomain, :token => @registration.auth_token) %>

Your authentication token is: <%= @registration.auth_token %>

The <%= AppConfig.title %> Team
<%= root_url %>

